<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysBannerMapper">

    <resultMap type="SysBanner" id="SysBannerResult">
        <result property="bannerId"       column="banner_id"       />
        <result property="title"          column="title"           />
        <result property="imageUrl"       column="image_url"       />
        <result property="linkType"       column="link_type"       />
        <result property="linkValue"      column="link_value"      />
        <result property="sort"           column="sort"            />
        <result property="status"         column="status"          />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
        <result property="remark"         column="remark"          />
    </resultMap>

    <sql id="selectBannerVo">
        select banner_id, title, image_url, link_type, link_value, sort, status, create_by, create_time, update_by, update_time, remark
        from sys_banner
    </sql>

    <select id="selectBannerById" parameterType="Long" resultMap="SysBannerResult">
        <include refid="selectBannerVo"/>
        where banner_id = #{bannerId}
    </select>

    <select id="selectBannerList" parameterType="SysBanner" resultMap="SysBannerResult">
        <include refid="selectBannerVo"/>
        <where>
            <if test="title != null and title != ''">
                AND title like concat('%', #{title}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by sort asc, create_time desc
    </select>

    <insert id="insertBanner" parameterType="SysBanner" useGeneratedKeys="true" keyProperty="bannerId">
        insert into sys_banner (
            <if test="title != null and title != '' ">title, </if>
            <if test="imageUrl != null and imageUrl != '' ">image_url, </if>
            <if test="linkType != null and linkType != '' ">link_type, </if>
            <if test="linkValue != null and linkValue != '' ">link_value, </if>
            <if test="sort != null">sort, </if>
            <if test="status != null and status != '' ">status, </if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="title != null and title != ''">#{title}, </if>
            <if test="imageUrl != null and imageUrl != ''">#{imageUrl}, </if>
            <if test="linkType != null and linkType != ''">#{linkType}, </if>
            <if test="linkValue != null and linkValue != ''">#{linkValue}, </if>
            <if test="sort != null">#{sort}, </if>
            <if test="status != null and status != ''">#{status}, </if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateBanner" parameterType="SysBanner">
        update sys_banner
        <set>
            <if test="title != null and title != ''">title = #{title}, </if>
            <if test="imageUrl != null and imageUrl != ''">image_url = #{imageUrl}, </if>
            <if test="linkType != null and linkType != ''">link_type = #{linkType}, </if>
            <if test="linkValue != null">link_value = #{linkValue}, </if>
            <if test="sort != null">sort = #{sort}, </if>
            <if test="status != null and status != ''">status = #{status}, </if>
            <if test="remark != null">remark = #{remark}, </if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where banner_id = #{bannerId}
    </update>

    <delete id="deleteBannerById" parameterType="Long">
        delete from sys_banner where banner_id = #{bannerId}
    </delete>

    <delete id="deleteBannerByIds" parameterType="Long">
        delete from sys_banner where banner_id in
        <foreach item="bannerId" collection="array" open="(" separator="," close=")">
            #{bannerId}
        </foreach>
    </delete>

</mapper>
